
  # Extract Components from Design

  This is a code bundle for Extract Components from Design. The original project is available at https://www.figma.com/design/7v7hODAaUnJrU1yXokbxms/Extract-Components-from-Design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  